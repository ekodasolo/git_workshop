# Git基礎講座 仕様書（SPEC）

## 概要

本講座は、Gitを初めて使う受講者を対象とした基礎講座です。Gitの基本操作からブランチ運用、やり直し操作、リモートリポジトリ（AWS CodeCommit）との連携までをカバーし、受講者が実務でGitを使い始められる状態をゴールとします。

## 実習環境

実習はLinux上で動作するCode Serverを使用します。Git操作はCode ServerのGUI機能ではなく、ターミナルからのコマンド入力を基本とします。これは「何が起きているか」をブラックボックスにせず、コマンドの意味を理解しながら進めるためです。Code ServerのMarkdownプレビュー機能は、ファイルの編集結果を確認する用途で活用します。

## 実習題材

### コンセプト

実習の題材には、架空のグルメバーガー店 **「AND BURGER」** のメニューサイト（Markdownドキュメント）を使用します。

題材にMarkdownドキュメントを選定した理由は以下の通りです。

Markdownはファイルの記法がシンプルで、テキストエディタで直感的に編集できるため、Git操作の学習から注意がそれることがありません。プログラミングコード（Pythonなど）を題材にすると「このコードは何をしているのか」という余計な認知負荷が発生しますが、飲食店のメニューという日常的な内容であれば、受講者全員が編集内容を即座に理解できます。ファイル間のリンクによる相互参照があることで、ブランチでの変更やコンフリクトの再現も自然に行えます。

### ファイル構成（5ファイル）

| ファイル名 | 内容 | 役割 |
|---|---|---|
| README.md | 店舗トップページ（店名、コンセプト、各ページへのリンク） | 講座を通じて最も頻繁に更新されるハブファイル |
| burgers.md | バーガーメニュー（名前、説明、価格） | メインコンテンツ。メニュー追加・価格変更の題材 |
| sides.md | サイドメニュー（フレンチフライ、オニオンリングなど） | ブランチ演習で追加するファイル |
| drinks.md | ドリンクメニュー（クラフトビール、レモネードなど） | ブランチ演習で追加するファイル |
| info.md | 店舗情報（営業時間、アクセス、SNSリンク） | ブランチ演習で追加するファイル |

各ファイルにはREADME.mdへの「トップに戻る」リンクや、関連ファイルへの相互リンク（例：burgers.md末尾に「サイドメニューはこちら → sides.md」）を含めます。これにより、Markdownのリンク記法を自然に練習でき、ファイル間の依存関係がGit操作の意味を実感させる効果を持ちます。

### 初期テンプレート

受講者がMarkdownを一から書く負荷を減らすために、各ファイルの骨格をテンプレートとして用意します。見出しやリンクの構造はあらかじめ記載しておき、メニュー項目の中身を受講者に埋めてもらう形とします。これにより「何を書けばいいかわからない」という迷いを排除し、Git操作への集中を確保します。

### リカバリー用スナップショット

各章の開始時点でのリポジトリの「あるべき状態」をスナップショットとして用意します。受講者が途中でつまずいた場合に、正しい状態のリポジトリを `git clone` してリカバリーできるようにし、講座全体の進行が止まるリスクを低減します。

---

## 講座構成

### 第1章：Gitを使う準備

この章では、Gitを使い始めるための土台を作ります。

#### 1-1. バージョン管理の必要性（導入）

ファイル名に「_最終版」「_最終版2」と付けた経験から入り、「履歴を安全に管理する仕組み」としてGitを紹介します。受講者のモチベーション形成が目的です。

#### 1-2. Gitのインストール

`git --version` でインストール確認するところまでをゴールとします。実習環境（Code Server）では事前にインストール済みのため、確認のみ行います。

#### 1-3. Gitの初期設定

`git config --global user.name` と `user.email` の設定を行います。「なぜこの設定が必要なのか（コミットに名前が記録される）」という理由をセットで伝えます。`git config --list` での設定確認も扱います。

**使用コマンド：** `git --version`、`git config --global`、`git config --list`

---

### 第2章：ローカルリポジトリでの基本操作

講座の核心部分です。Gitの基本サイクル（編集→ステージング→コミット）を体で覚えてもらいます。

#### 2-1. ローカルリポジトリの新規作成

`git init` でリポジトリを作成します。ここで「ワークツリー」「ステージングエリア」「Gitディレクトリ（リポジトリ）」という3つの領域の概念図を示します。この3層構造の理解が、初心者がGitでつまずくかどうかの最大の分かれ道です。

**実習内容：** 作業用ディレクトリを作成し、`git init` を実行する。

#### 2-2. Gitの基本フロー — add と commit

`git add` でステージングエリアに登録し、`git commit` でコミットする流れを体験します。

**実習内容：**

1. README.mdを作成し、最初のコミットを行う。
2. burgers.mdを作成し、README.mdにリンクを追加して2回目のコミットを行う。

**使用コマンド：** `git add`、`git add -A`、`git commit -m`

#### 2-3. 状態確認のコマンド — status, diff, log

「今どうなっているか確認する」習慣を身につけてもらいます。`git status` は「困ったらまず実行するコマンド」として位置づけます。

**実習内容：** burgers.mdの内容を編集し、`git status` で状態確認、`git diff` で変更内容の確認、コミット後に `git log` で履歴確認を行う。`git diff --cached` でステージングエリアとGitディレクトリの差分も確認し、3層構造の理解を深める。

**使用コマンド：** `git status`、`git diff`、`git diff --cached`、`git log`、`git log -p`

#### 2-4. コミット前のやり直し

「間違えても戻せる」という安心感を持ってもらうことが目的です。やり直し操作の第1段階として、コミットする前に取り消せる2つのパターンを扱います。

**実習内容：** ワークツリーの変更を `git restore` で取り消す操作と、`git restore --staged` でステージング解除する操作を体験する。

**使用コマンド：** `git restore`、`git restore --staged`

---

### 第3章：リモートリポジトリとの連携（CodeCommit編）

ローカルだけで完結していた作業を、チームで共有できるようにするステップです。

#### 3-1. リモートリポジトリとは

「ローカルリポジトリがPC上のもの、リモートリポジトリがサーバー上のもの」という関係を図で示します。連携先としてAWS CodeCommitを使用することを明示します。

#### 3-2. CodeCommitでリモートリポジトリを作成

CodeCommitのコンソールでリポジトリを作成し、接続に必要な認証設定を済ませます。事前準備として講座前に完了させておくことも検討します。

#### 3-3. ローカルリポジトリのプッシュ

`git remote add` でリモートリポジトリを登録し、`git remote -v` で設定を確認してから、`git push` で初回プッシュを行います。

**実習内容：** 第2章で作成したリポジトリ（README.mdとburgers.md）をCodeCommitにプッシュする。

**使用コマンド：** `git remote add`、`git remote -v`、`git push`

---

### 第4章：ブランチの基本

ブランチの概念と操作方法を学びます。ブランチ戦略の全体像を理解したうえで、実際の操作に入ります。

#### 4-1. ブランチとは

「本流（統合ブランチ）から枝分かれして、安全に作業するための仕組み」として説明します。mainブランチを幹、トピックブランチを枝に見立てた図を使用します。「なぜブランチを使うのか（本番環境を壊さずに開発できる）」という動機づけが重要です。

#### 4-2. ブランチ戦略の概要 — Git Flowを知る

なぜブランチの運用ルール（戦略）が必要なのかを説明してから、Git Flowの全体像を紹介します。

Git Flowの5つのブランチ（main、develop、feature、release、hotfix）の役割を図で簡潔に説明します。ただし深入りはせず、「今回の講座ではmain（統合ブランチ）とfeature（トピックブランチ）の関係に絞って体験する」と範囲を宣言します。

GitHub FlowやGitLab Flowといった軽量な戦略の存在にも触れ、「今回の講座で体験する流れはGitHub Flowに近い」と伝えます。これにより受講者は「Git Flowが唯一の正解ではない」という視野を持てます。

#### 4-3. ブランチ操作の基本コマンド

`git branch`（作成・一覧表示）、`git switch`（切り替え）、`git switch -c`（作成と切り替えを同時に）を扱います。`git checkout` との関係にも触れつつ、`git switch` を推奨します。

**使用コマンド：** `git branch`、`git switch`、`git switch -c`、`git checkout`（参考）

#### 4-4. トピックブランチの作成とマージ

featureブランチを使って残りのファイルを追加し、mainにマージする一連の流れを繰り返し体験します。

**実習内容：**

1. `add-sides` ブランチを作成し、sides.mdを作成、README.mdにリンクを追加、mainにマージ。
2. `add-drinks` ブランチを作成し、drinks.mdを作成、README.mdにリンクを追加、mainにマージ。
3. `add-info` ブランチを作成し、info.mdを作成、README.mdにリンクを追加、mainにマージ。

各サイクルでマージ後に `git branch -d` でブランチを削除するところまでを1セットとします。

**使用コマンド：** `git switch -c`、`git add`、`git commit -m`、`git switch`、`git merge`、`git branch -d`、`git push`

---

### 第5章：コミット後のやり直し（プッシュ前）

やり直し操作の第2段階です。コミットはしたがまだリモートにプッシュしていない状態で、コミットを修正・取り消す方法を学びます。第2章で学んだ3層構造（ワークツリー・ステージングエリア・Gitディレクトリ）の理解がここで活きてきます。

#### 5-1. 直前のコミットの修正 — git commit --amend

直前のコミットメッセージを修正したい場合や、ファイルをコミットし忘れた場合に使います。最も軽量なやり直し操作であり、「さっきのコミット、ちょっと直したい」という日常的な場面に対応します。

**実習シナリオ：** burgers.mdに新メニューを追加してコミットした後、コミットメッセージに誤字があったことに気づき、`git commit --amend -m` で修正する。続いて、メニューの説明文を書き忘れていたことに気づき、ファイルを修正して `git add` → `git commit --amend` で直前のコミットに含める。

**使用コマンド：** `git commit --amend`、`git commit --amend -m "メッセージ"`

#### 5-2. コミットの取り消し — git reset

コミット自体を取り消す操作です。`git reset` には3つのモードがあり、それぞれコミット履歴・ステージングエリア・ワークツリーのどこまで巻き戻すかが異なります。3つのモードの違いを3層構造の図と対応させて説明することで、受講者の理解が格段に深まります。

**3つのモードの整理：**

| モード | コミット履歴 | ステージングエリア | ワークツリー | 主な用途 |
|---|---|---|---|---|
| `--soft` | 巻き戻す | そのまま | そのまま | コミットだけやり直したい（変更内容はステージングに残る） |
| `--mixed`（デフォルト） | 巻き戻す | 巻き戻す | そのまま | コミットとステージングをやり直したい（変更内容はワークツリーに残る） |
| `--hard` | 巻き戻す | 巻き戻す | 巻き戻す | すべてを完全に巻き戻したい（変更内容も消える） |

**実習シナリオ：** burgers.mdに「期間限定バーガー」を追加してコミットした後、「やっぱりこのメニューは追加しない」と判断した設定で、各モードの動作を順に体験する。

1. `git reset --soft HEAD~1` を実行し、コミットは消えたがステージングエリアに変更が残っていることを `git status` で確認する。
2. `git commit` で再度コミットして状態を戻してから、`git reset --mixed HEAD~1` を実行し、コミットとステージングが消えたがワークツリーに変更が残っていることを確認する。
3. `git add` → `git commit` で状態を戻してから、`git reset --hard HEAD~1` を実行し、すべてが巻き戻されたことを確認する。

**使用コマンド：** `git reset --soft`、`git reset --mixed`、`git reset --hard`

#### 5-3. なぜプッシュ前に限定するのか

`git reset` はコミット履歴を書き換える操作です。すでにリモートにプッシュした内容に対して使うと、他のメンバーとの履歴が食い違い、深刻な問題を引き起こします。この「履歴の書き換えはプッシュ前まで」というルールをここで明確に伝え、次章（プッシュ後のやり直し）への導入とします。

---

### 第6章：プッシュ後のやり直し

やり直し操作の第3段階です。リモートにプッシュ済みの変更を安全に取り消す方法を学びます。

#### 6-1. 打ち消しコミットの作成 — git revert

`git revert` は、過去のコミットの変更内容を打ち消す新しいコミットを作る操作です。`git reset` が「履歴を書き換える」のに対して、`git revert` は「履歴に追記する」という根本的な違いがあります。履歴を壊さないため、プッシュ済みの変更でも安全に取り消せます。

**実習シナリオ：** burgers.mdに新メニュー「スモークベーコンバーガー」を追加してコミット・プッシュした後、「この新メニューは不評だったので取り下げたい」という設定で `git revert` を体験する。

1. `git log --oneline` で対象のコミットハッシュを確認する。
2. `git revert コミットハッシュ` を実行し、打ち消しコミットが作成されることを確認する。
3. `git log` で履歴を確認し、元のコミットが消えずに残ったまま、新しい打ち消しコミットが追加されていることを確認する。
4. `git push` で打ち消しコミットをリモートに反映する。

**使用コマンド：** `git revert コミットハッシュ`、`git log --oneline`

#### 6-2. reset と revert の使い分け

第5章と第6章で学んだやり直し操作を整理し、状況に応じた使い分けのルールをまとめます。「プッシュ済みかどうか」が最大の判断基準であることを再確認します。

---

### やり直し操作の全体整理

第2章・第5章・第6章にまたがるやり直し操作を、状況別に1つの表に整理します。この表は受講者に配布する資料として、講座後のリファレンスにもなります。

| 状況 | やりたいこと | 使うコマンド | 学習する章 |
|---|---|---|---|
| コミット前・ステージング前 | ワークツリーの変更を取り消したい | `git restore ファイル名` | 第2章 |
| コミット前・ステージング後 | ステージングを取り消したい | `git restore --staged ファイル名` | 第2章 |
| コミット後・プッシュ前 | 直前のコミットメッセージを直したい | `git commit --amend -m "メッセージ"` | 第5章 |
| コミット後・プッシュ前 | 直前のコミットにファイルを追加し忘れた | `git add` → `git commit --amend` | 第5章 |
| コミット後・プッシュ前 | コミット自体を取り消したい（変更は残す） | `git reset --soft HEAD~1` | 第5章 |
| コミット後・プッシュ前 | コミットとステージングを取り消したい（変更は残す） | `git reset --mixed HEAD~1` | 第5章 |
| コミット後・プッシュ前 | すべてを完全に巻き戻したい | `git reset --hard HEAD~1` | 第5章 |
| プッシュ後 | プッシュ済みの変更を安全に取り消したい | `git revert コミットハッシュ` | 第6章 |

---

### 第7章：コンフリクトの理解と対応

初心者が最も不安に感じるテーマですが、「怖くない、手順通りにやれば解消できる」というメッセージを伝えることが重要です。

#### 7-1. コンフリクトとは

同じファイルの同じ箇所を別々のブランチで変更したときに発生する、という状況を具体例で示します。コンフリクトマーカー（`<<<<<<<`、`=======`、`>>>>>>>`）の読み方を丁寧に解説します。

#### 7-2. コンフリクトへの対応

意図的にコンフリクトを起こし、手動で解消する手順を体験します。

**実習シナリオ：**

1. mainブランチでburgers.mdの「クラシックバーガー」の価格を1,200円から1,300円に変更してコミットする。
2. `update-menu` ブランチを作成し、同じ「クラシックバーガー」の価格を1,500円に変更してコミットする。
3. mainブランチに戻り、`update-menu` ブランチをマージしようとすると、同じ行が異なる内容に変更されているためコンフリクトが発生する。
4. コンフリクトマーカーを確認し、どちらの価格を採用するか判断して手動で解消する。
5. `git add` → `git commit` でマージを完了する。

`git merge --abort` で「やっぱり中止する」選択肢があることも伝えます。

**補足：** 第6章で学んだ `git revert` を実行した際にもコンフリクトが発生する可能性があることに軽く触れ、やり直し操作とコンフリクト対応の知識をつなげます。

**使用コマンド：** `git merge`、`git merge --abort`、`git add`、`git commit`

---

### 第8章：リモートリポジトリからの取得

#### 8-1. git clone — 既存リポジトリのコピー

`git clone` でリモートリポジトリをローカルにコピーする方法を扱います。新しくプロジェクトに参加するときの最初のステップとして位置づけます。

**実習内容：** CodeCommit上の完成したリポジトリを別のディレクトリにcloneし、全ファイルが取得できることを確認する。

**使用コマンド：** `git clone`

#### 8-2. git pull と git fetch

`git pull`（取得＋マージ）と `git fetch`（取得のみ）の違いを説明します。初心者にはまず `git pull` を教え、慣れてきたら `git fetch` + `git merge` という2段階の方法もあると紹介します。

**使用コマンド：** `git pull`、`git fetch`

---

### 第9章：その他知っておきたいコマンド・Tips

#### 9-1. よく使う便利コマンド

`git rm`（ファイル削除）、`git commit -am`（ステージングとコミットの同時実行）、`git log -p`（差分付き履歴表示）など、作業効率を上げるコマンドをまとめて紹介します。

**使用コマンド：** `git rm`、`git rm -r`、`git commit -a`、`git commit -am`、`git log -p`

#### 9-2. .gitignore の活用

管理対象外にしたいファイルを指定する `.gitignore` の書き方と、`git rm --cached` で既に追跡中のファイルを管理外にする方法を扱います。

**使用コマンド：** `git rm --cached`

#### 9-3. まとめと Next Step

講座全体の復習として、Gitの基本フロー（init → add → commit → push → pull）を1枚の図に整理します。次に学ぶとよいトピックとして以下を紹介して締めくくります。

- Git Flowの残りのブランチ（develop、release、hotfix）
- プルリクエスト（マージリクエスト）
- リベース
- タグ

---

## コマンド一覧（講座で使用するもの）

### 第1章：Gitを使う準備

| コマンド | 用途 |
|---|---|
| `git --version` | Gitのバージョン確認 |
| `git config --global user.name "名前"` | ユーザー名の設定 |
| `git config --global user.email "メール"` | メールアドレスの設定 |
| `git config --list` | 設定値の一覧表示 |

### 第2章：ローカルリポジトリでの基本操作

| コマンド | 用途 |
|---|---|
| `git init` | ローカルリポジトリの作成 |
| `git add ファイル名` | 指定ファイルをステージングエリアへ登録 |
| `git add -A` | 全変更をまとめてステージングエリアへ登録 |
| `git commit -m "メッセージ"` | コミットメッセージを指定してコミット |
| `git status` | ローカルリポジトリの状態確認 |
| `git diff` | ワークツリーとステージングエリアの差分表示 |
| `git diff --cached` | ステージングエリアとGitディレクトリの差分表示 |
| `git log` | コミット履歴の表示 |
| `git log -p` | 差分付きコミット履歴の表示 |
| `git restore ファイル名` | ワークツリーの変更を取り消し |
| `git restore --staged ファイル名` | ステージングエリアへの登録を取り消し |

### 第3章：リモートリポジトリとの連携（CodeCommit編）

| コマンド | 用途 |
|---|---|
| `git remote add リモート名 URL` | リモートリポジトリの登録 |
| `git remote -v` | リモートリポジトリの設定確認 |
| `git push origin ブランチ名` | ローカルの内容をリモートに反映 |

### 第4章：ブランチの基本

| コマンド | 用途 |
|---|---|
| `git branch` | ブランチの一覧表示 |
| `git branch ブランチ名` | ブランチの作成 |
| `git switch ブランチ名` | ブランチの切り替え |
| `git switch -c ブランチ名` | ブランチの作成と切り替えを同時に実行 |
| `git merge ブランチ名` | 指定ブランチを現在のブランチにマージ |
| `git branch -d ブランチ名` | マージ済みブランチの削除 |

### 第5章：コミット後のやり直し（プッシュ前）

| コマンド | 用途 |
|---|---|
| `git commit --amend` | 直前のコミットを修正（エディタが開く） |
| `git commit --amend -m "メッセージ"` | 直前のコミットメッセージを変更 |
| `git reset --soft HEAD~1` | コミットのみ取り消し（ステージング・ワークツリーはそのまま） |
| `git reset --mixed HEAD~1` | コミットとステージングを取り消し（ワークツリーはそのまま） |
| `git reset --hard HEAD~1` | コミット・ステージング・ワークツリーをすべて巻き戻し |

### 第6章：プッシュ後のやり直し

| コマンド | 用途 |
|---|---|
| `git revert コミットハッシュ` | 指定コミットを打ち消す新しいコミットを作成 |
| `git log --oneline` | コミット履歴を1行ずつ簡潔に表示 |

### 第7章：コンフリクトの理解と対応

| コマンド | 用途 |
|---|---|
| `git merge --abort` | マージの中止 |

### 第8章：リモートリポジトリからの取得

| コマンド | 用途 |
|---|---|
| `git clone URL` | リモートリポジトリのコピー |
| `git pull origin ブランチ名` | リモートの内容を取得しワークツリーに反映 |
| `git fetch リモート名` | リモートの内容をローカルリポジトリに取得（ワークツリーは変更しない） |

### 第9章：その他知っておきたいコマンド・Tips

| コマンド | 用途 |
|---|---|
| `git rm ファイル名` | ファイルの削除 |
| `git rm -r ディレクトリ名` | ディレクトリの削除 |
| `git rm --cached ファイル名` | Gitの管理下から外す（ファイルは残る） |
| `git commit -a` | ステージングとコミットの同時実行 |
| `git commit -am "メッセージ"` | ステージングとコミットメッセージ指定の同時実行 |

---

## 構成上の設計意図

### やり直し操作を3段階に分けた理由

やり直し操作は受講者の知識が積み上がったタイミングで段階的に教える構成としています。`git reset` を理解するにはコミット履歴の概念がしっかり身についている必要があり、`git revert` の意味を実感するにはリモートリポジトリとの関係（プッシュ済みかどうか）がわかっている必要があるためです。

第1段階（第2章）ではコミット前の取り消しとして `git restore` / `git restore --staged` を扱い、第2段階（第5章）ではコミット後・プッシュ前の操作として `git commit --amend` / `git reset` を扱い、第3段階（第6章）ではプッシュ後の操作として `git revert` を扱います。

### コンフリクト（第7章）の配置理由

コンフリクトをrevert（第6章）の後に配置したのは、`git revert` を実行するとコンフリクトが発生する場合があるためです。先にrevertを学んでおくことで、コンフリクトの章で「revert時のコンフリクト」にも触れることができ、やり直し操作とコンフリクト対応の知識がつながります。

### ブランチ戦略（第4章）の扱い方

Git Flowの全体像を俯瞰で示しつつ、講座で実際に体験する範囲はmain（統合ブランチ）とfeature（トピックブランチ）に絞っています。GitHub Flowにも触れることで、「Git Flowが唯一の正解ではない」という視野を持たせます。

---

## 今後の作業

1. 各ファイルの初期テンプレートの作成（README.md、burgers.md、sides.md、drinks.md、info.md）
2. 各章の開始時点でのリポジトリスナップショットの作成
3. 講座スライド・テキストの作成
4. コンフリクト演習用のシナリオ詳細設計
5. やり直し操作演習用のシナリオ詳細設計（第5章・第6章）
6. CodeCommit接続手順の整理（認証設定含む）
